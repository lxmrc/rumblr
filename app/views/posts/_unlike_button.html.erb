<% if user_signed_in? %>
  <%= link_to unlike_post_path(post),
    method: :delete,
    remote: true,
    class: 'btn btn-danger btn-sm',
    id: "unlike-#{post.id}" do %>
      <i class="far fa-heart"></i>
  <% end %>
<% end %>

